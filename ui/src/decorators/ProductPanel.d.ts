/**
 * @explain
 * This storybook decorator is used to add a background color, padding to the story and limit the width to make it look like a product panel.
 */
import { FC } from 'react';
export declare const ProductPanel: (Story: FC) => import("react/jsx-runtime").JSX.Element;
